<div class="banner-list">
  <img class="img-list" src=<%= @list.photo.url %>>
    <div class="banner-list-text text-center">
      <h1><%= @list.name %></h1>
      <p>Choose movies and add them to your list</p>
      <%= link_to "Add a new movie", new_list_bookmark_path(@list), class: "btn btn-gradient" %>
    </div>
</div>
